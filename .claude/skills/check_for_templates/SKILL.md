---
name: check_for_templates
description: Check if any code examples that can be used as stubs
model: sonnet
---

If the project has a folder named `_reference/templates` then there might be some code examples that can be use
top create new code stubs.

Inside the template folders:

* nuxt - nuxt templates
* vue - vue templates
