services:
  escaroll:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - ESCAROLL_PRINTER_IP=${ESCAROLL_PRINTER_IP:-192.168.1.17}
      - ESCAROLL_TEMPLATES_PATH=/data/templates
    volumes:
      - ./data:/data
    restart: unless-stopped
